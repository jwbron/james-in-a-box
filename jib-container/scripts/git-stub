#!/bin/bash
#
# Stub for /usr/bin/git - blocks direct access
#
# This stub replaces /usr/bin/git to prevent bypassing the git wrapper.
# The real git binary has been relocated to prevent direct access.
#
# Always use 'git' (from PATH) which routes through the gateway sidecar.
#

echo "ERROR: Direct /usr/bin/git access is blocked." >&2
echo "" >&2
echo "Use 'git' (without path) which routes through the gateway sidecar." >&2
echo "The gateway enforces security policies and provides authentication." >&2
echo "" >&2
echo "Example: git push origin my-branch" >&2
exit 1
