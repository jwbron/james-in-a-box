# JIB Container

Container infrastructure and components that run inside the container.

## Components

- **context-watcher/** - Monitor `~/context-sync/` for changes

## Configuration

- **.claude/** - Claude configuration (slash commands, rules)
- **docker-setup.py** - Container setup script
